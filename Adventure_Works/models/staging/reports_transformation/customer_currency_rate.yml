
version: 2

model:
  - name: customer_currency_rate
    columns:
      - name: averagerate
        tests:
          - not_null
          - number_greater_than:
              min_num : 1000
